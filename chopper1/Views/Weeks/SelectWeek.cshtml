@model string

@{    
    Layout = null;    
    ViewBag.Title = "SelectWeek";
    var curweeknum = ViewBag.weeknum;
    var curweeknum2 = ViewBag.WeekId;
    string repType = ViewBag.reportType.ToString();
    string KanalKod = ViewBag.chCode.ToString();
    /*
    if (repType=="raskladka")
    {
        Html.RenderAction("getWeek", "Weeks", new { week_num = curweeknum});
    }
    else
    {
        if (repType == "stolby")
        {
            Html.RenderAction("Stolby", "Weeks", new { week_num = curweeknum });
        }
    }
    */
    switch (repType)
    {
        case "orbity":
            Html.RenderAction("OrbWeek", "Weeks", new { week_num = curweeknum });
            break;
        case "raskladka":
            Html.RenderAction("getWeek", "Weeks", new { week_num = curweeknum, chCode = KanalKod });
            break;
        case "stolby":
            Html.RenderAction("Stolby", "Weeks", new { week_num = curweeknum });
            break;
    }
}

